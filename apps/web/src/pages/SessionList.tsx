import { useEffect, useState } from 'react';import { useNavigate } from 'react-router-dom';import { useStore } from '../store/useStore';import { chatApi } from '../lib/api';import dayjs from 'dayjs';import relativeTime from 'dayjs/plugin/relativeTime';import 'dayjs/locale/zh-cn';dayjs.extend(relativeTime);dayjs.locale('zh-cn');export default function SessionList() {  const navigate = useNavigate();  const { sessions, fetchSessions, user, setCurrentSession, isOnline, chatMeta, friends } = useStore();  const [otherMap, setOtherMap] = useState<Record<string, { nickname: string; avatarUrl: string; id: string }>>({});  useEffect(() => { fetchSessions(); }, []);  useEffect(() => {    if (!user || sessions.length === 0) return;    const singles = sessions.filter(s => s.type === 'single' && s.otherMembers.length === 0 && !otherMap[s.id]);    if (singles.length === 0) return;    (async () => {      const updates: Record<string, { nickname: string; avatarUrl: string; id: string }> = {};      for (const s of singles) {        try {          const msgs = await chatApi.getMessages(s.id);          for (const m of msgs) {            if (m.senderId !== user.id && m.sender) {              updates[s.id] = { nickname: m.sender.nickname || '未知', avatarUrl: m.sender.avatarUrl || '', id: m.senderId };              break;            }          }          if (!updates[s.id]) {            updates[s.id] = { nickname: '未知', avatarUrl: '', id: '' };          }        } catch (e) {          console.error('Failed to resolve other member for session', s.id, e);          updates[s.id] = { nickname: '未知', avatarUrl: '', id: '' };        }      }      if (Object.keys(updates).length > 0) {        setOtherMap(prev => ({ ...prev, ...updates }));      }    })();  }, [sessions, user?.id]);  const handleOpen = (sessionId: string) => {    setCurrentSession(sessionId);    navigate('/chat/' + sessionId);  };  const getOther = (s: any) => {    if (s.otherMembers.length > 0) return s.otherMembers[0];    return chatMeta?.[s.id] || otherMap[s.id] || (friends||[]).find((f: any) => f.id === (s.members||[]).find((m: string) => m !== user?.id)) || null;  };  return (    <div className="h-full flex flex-col bg-white page-enter">      <div className="px-5 pt-14 pb-4 bg-white">        <div className="flex items-center justify-between">          <h1 className="text-2xl font-bold text-text tracking-tight">消息</h1>          <div className="w-9 h-9 rounded-full bg-primary/10 flex items-center justify-center text-primary font-bold text-sm">            {user?.nickname?.[0] || '?'}          </div>        </div>      </div>      <div className="flex-1 overflow-y-auto">        {sessions.length === 0 ? (          <div className="flex flex-col items-center justify-center h-full text-text-secondary/50">            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>            </svg>            <p className="text-sm mt-3">޻Ự</p>          </div>        ) : (          sessions.map(s => {            const other = getOther(s);            const displayName = s.type === 'group' ? (s.name || '群聊') : (other?.nickname || '未知');            const online = other?.id ? isOnline(other.id) : false;            const lastMsg = s.lastMessage;            const lastText = lastMsg              ? (lastMsg.type === 'text' ? lastMsg.content : lastMsg.type === 'voice' ? '[语音]' : lastMsg.type === 'image' ? '[图片]' : '[文件]')              : '暂无会话消息';            return (              <div                key={s.id}                onClick={() => handleOpen(s.id)}                className="flex items-center gap-3.5 px-5 py-3.5 active:bg-gray-50 transition-colors cursor-pointer"              >                <div className="relative flex-shrink-0">                  <div className={'w-13 h-13 rounded-2xl flex items-center justify-center text-white font-bold text-lg overflow-hidden ' + (s.type === 'group' ? 'bg-gradient-to-br from-amber-400 to-orange-500' : 'bg-gradient-to-br from-blue-400 to-primary')}>                    {other?.avatarUrl ? (                      <img src={other.avatarUrl} alt="" className="w-13 h-13 rounded-2xl object-cover" />                    ) : (                      <span>{displayName[0]}</span>                    )}                  </div>                  {s.type === 'single' && online && (                    <div className="absolute -bottom-0.5 -right-0.5 w-3.5 h-3.5 bg-online rounded-full border-2 border-white" />                  )}                </div>                <div className="flex-1 min-w-0 border-b border-border pb-3.5">                  <div className="flex items-center justify-between">                    <span className="font-semibold text-text truncate text-[15px]">{displayName}</span>                    <span className="text-xs text-text-secondary/60 flex-shrink-0 ml-2">{lastMsg ? dayjs(lastMsg.createdAt).fromNow() : ""}</span>                  </div>                  <p className="text-sm text-text-secondary truncate mt-0.5">{lastText}</p>                </div>              </div>            );          })        )}      </div>    </div>  );}